version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "Installing requirements..."
      - pip3 install --no-cache-dir -r requirements.txt
      - echo "Installed packages..."
      - pip3 list
      - chmod +x startup.sh

run:
  command: ./startup.sh
  network:
    port: 8000
