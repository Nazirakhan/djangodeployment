version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
      - chmod +x startup.sh

run:
  runtime-version: 3.11.7
  command: /bin/bash startup.sh  # Explicitly use bash
  network:
    port: 8000  # Matches $PORT environment variable